---
// 1. AÑADIMOS ESTAS DOS LÍNEAS
import { useTranslations } from '../../i18n/utils';
const t = useTranslations(Astro.currentLocale as 'es' | 'en');
---
<!-- SECCIÓN "MI HISTORIA" -->
<section id="mi-historia" class="section-spacing">
  <div class="max-w-5xl mx-auto px-8">
    <h2 class="section-title text-slate-900 dark:text-slate-100">
      <span class="icon-emoji">🚀</span>
      <!-- 2. CAMBIAMOS EL TÍTULO -->
      {t('history.title')}
    </h2>
    <div class="card-gradient">
      <!-- 3. REEMPLAZAMOS EL PÁRRAFO 1 -->
      <p class="text-slate-700 dark:text-slate-300 leading-relaxed mb-4 text-lg">
        {t('history.p1_start')}
        <strong class="text-blue-500 dark:text-blue-400">{t('history.p1_psychology')}</strong>
        {t('history.p1_end')}
        <strong class="text-purple-500 dark:text-purple-400">{t('history.p1_technology')}</strong>
        {t('history.p1_dot')}
      </p>
      <!-- 4. REEMPLAZAMOS EL PÁRRAFO 2 -->
      <p class="text-slate-700 dark:text-slate-300 leading-relaxed mb-4 text-lg">
        {t('history.p2')}
      </p>
      <!-- 5. REEMPLAZAMOS EL PÁRRAFO 3 -->
      <p class="text-slate-700 dark:text-slate-300 leading-relaxed text-lg">
        {t('history.p3_start')}
        <strong class="text-gradient-blue-purple">{t('history.p3_role')}</strong>
        {t('history.p3_end')}
      </p>
    </div>
  </div>
</section>

